# 核心依赖（用于数据分析和可视化）
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.4.0
seaborn>=0.11.0
scipy>=1.7.0

# 文档处理
python-docx>=0.8.11
openpyxl>=3.0.7
PyYAML>=5.4.1

# PDF处理（可选，用于视觉理解题目）
PyMuPDF>=1.21.0

# 知识库与向量检索（可选）
sentence-transformers>=2.2.0
scikit-learn>=1.0.0

# 网络请求
httpx>=0.24.0

# 知识图谱 Neo4j 驱动
neo4j>=5.0.0

# ===== 模型训练（算法岗新增） =====
# 核心训练框架
transformers>=4.40.0
peft>=0.10.0              # QLoRA 微调
datasets>=2.19.0          # DPO 训练数据
trl>=0.8.0                # DPO trainer
accelerate>=0.30.0        # 分布式训练
bitsandbytes>=0.43.0      # 4-bit/8-bit 量化

# Tokenizer
tiktoken>=0.7.0           # OpenAI 分词器

# 评估
rouge-score>=0.1.2        # ROUGE 评估
nltk>=3.8.0               # 自然语言处理工具

# 可选：LLaMA-Factory（推荐用于 QLoRA 训练）
# llamafactory>=0.8.0     # 取消注释以启用
